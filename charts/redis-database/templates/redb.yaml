apiVersion: app.redislabs.com/v1alpha1
kind: RedisEnterpriseDatabase
metadata:
  name: {{ .Values.name }}
  namespace: redis
spec:
  redisEnterpriseCluster:
    name: rec
  memorySize: {{ .Values.memorySize }}
  databasePort: {{ .Values.databasePort }}
  databaseSecretName: {{ .Values.databaseSecretName }}
